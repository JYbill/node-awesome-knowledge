datasource db {
  provider = "mongodb"
  // provider = "mysql"
  url      = "mongodb://xiaoqinvar:990415@www.jybill.top:27017,www.jybill.top:27018,www.jybill.top:27019/test?replicaSet=rs0&authSource=admin&connectTimeoutMS=5000"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id   String  @id @default(auto()) @map("_id") @db.ObjectId
  age  Int?
  name String?
}
